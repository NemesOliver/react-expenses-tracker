// Header will display total amount
import { useState } from "react";

const Header = () => {
  //total amount

  return (
    <header>
      <h1>£0</h1>
    </header>
  );
};

export default Header;

// GET AMOUNT DISPLAYED
